% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{train_causal}
\alias{train_causal}
\title{Train a Causal Model with Optional Cross-Validation}
\usage{
train_causal(
  data_G1,
  data_G2,
  lambda,
  target = "Y",
  loss_func = obj_func,
  model_func = NULL,
  hidden_sizes = NULL,
  method = "BFGS",
  verbose = TRUE,
  cv_folds = 1,
  ...
)
}
\arguments{
\item{data_G1}{A data frame containing the first environment's training data.}

\item{data_G2}{A data frame containing the second environment's training data.}

\item{lambda}{Numeric in [0,1] controlling the trade-off between the least-squares and
discrepancy terms.}

\item{target}{Character string indicating the name of the target variable in \code{data_G1} and \code{data_G2}.}

\item{loss_func}{A function that computes the objective (loss). Defaults to \code{\link{obj_func}}.}

\item{model_func}{A function that predicts \eqn{\hat{y}} given (weights, X, parameters). If \code{NULL},
it is chosen automatically from \code{\link{define_functional_forms}} based on \code{hidden_sizes}.}

\item{hidden_sizes}{An integer vector specifying the number of neurons in each hidden layer.
If \code{NULL} or empty, a linear model is assumed.}

\item{method}{The optimization method to be passed to \code{\link[stats]{optim}}. Default is \code{"BFGS"}.}

\item{verbose}{Logical; if \code{TRUE}, prints detailed configuration, progress, and timing information.}

\item{cv_folds}{Integer; if greater than 1, performs K–fold cross validation on the training data.
Defaults to 1 (i.e. no cross validation).}

\item{...}{Further arguments passed to \code{\link[stats]{optim}} (e.g., control parameters).}
}
\value{
A list containing:
\describe{
  \item{params}{The optimized model parameters (numeric vector) trained on the full data.}
  \item{model_func}{The function used for predictions (linear or neural network).}
  \item{hidden_sizes}{The hidden layer configuration (if any).}
  \item{target}{The name of the target variable used.}
  \item{cv_performance}{If cross validation was performed, the average RMSE across the CV folds. Otherwise, \code{NA}.}
}
}
\description{
Optimizes model parameters to minimize a causal objective function that balances the
overall least-squares error across two environments (E1 and E2) and a discrepancy term.
In addition to training on the full training data, an optional K–fold cross validation
can be performed to assess the model's training performance. The cross-validation is
done separately on each environment and the validation sets are merged for evaluation.

By default, the function uses \code{\link{obj_func}} as the loss function.
If \code{hidden_sizes} is \code{NULL} or empty, a linear model is assumed.
Otherwise, a feedforward neural network is used.
}
\examples{
\dontrun{
  # Train a linear causal model with 5-fold CV:
  model_linear <- train_causal(data_G1, data_G2, lambda = 0.3, target = "Y", cv_folds = 5)

  # Train a neural network causal model with 5-fold CV:
  model_nn <- train_causal(data_G1, data_G2, lambda = 0.3, target = "Y",
                           hidden_sizes = c(3, 3), cv_folds = 5)
}

}
