% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functional.R, R/functions.R
\name{obj_func}
\alias{obj_func}
\title{Objective function using combined MSE and CD term}
\usage{
obj_func(
  weights,
  data_G1,
  data_G2,
  lambda,
  model_func,
  parameters = NULL,
  target = "Y"
)

obj_func(
  weights,
  data_G1,
  data_G2,
  lambda,
  model_func,
  parameters = NULL,
  target = "Y"
)
}
\arguments{
\item{weights}{A numeric vector of model weights/parameters.}

\item{data_G1}{A data frame for environment 1. Must contain the target variable.}

\item{data_G2}{A data frame for environment 2. Must contain the target variable.}

\item{lambda}{A numeric value in [0,1] that controls the tradeoff between LS and CD.}

\item{model_func}{A function that predicts \eqn{\hat{y}} given (weights, X, parameters).}

\item{parameters}{An optional list of extra parameters for the model function (e.g. hidden layer sizes).}

\item{target}{A character string naming the target variable in \code{data_G1} and \code{data_G2}.}
}
\value{
The value of the combined objective function.

A single numeric value giving the objective function (loss).
}
\description{
This function calculates an objective function that combines Mean Squared Error (MSE)
and Causal Discrepancy (CD) between two different environments.

Computes a weighted objective that balances overall least-squares error across
two environments (E1 and E2) and the discrepancy in errors between them.

Specifically:
\enumerate{
  \item \strong{LS (Least-Squares) Term}: 
    \deqn{ f_{LS}(\beta) = \frac{1}{n_1 + n_2} \Bigl[\sum_{i=1}^{n_1}(Y_i^{(E1)} - f_{\beta}(X_i^{(E1)}))^2 
    + \sum_{i=1}^{n_2}(Y_i^{(E2)} - f_{\beta}(X_i^{(E2)}))^2\Bigr]. }
  \item \strong{CD (Causal Discrepancy) Term}:
    \deqn{ f_{CD}(\beta) = \Bigl| \mathrm{MSE}(E_1) - \mathrm{MSE}(E_2)\Bigr|. }
}

The final objective is:
\deqn{ (1-\lambda) \cdot f_{LS}(\beta) \;+\; \lambda \cdot f_{CD}(\beta). }
}
\examples{
# Suppose data_G1 and data_G2 each have a column "Y" as the target
# and columns X1, X2, etc. for predictors.
# model_func could be a linear or neural network predictor.
# lambda in [0,1].
#
# obj_value <- obj_func(weights = runif(5),
#                       data_G1 = env1_data,
#                       data_G2 = env2_data,
#                       lambda = 0.5,
#                       model_func = my_model_predictor,
#                       parameters = list(...),
#                       target = "Y")

}
